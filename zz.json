[
    {
        "server": "PVIDA50",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida50",
        "aplication_name": "Privilegiados PVIDA50",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA20",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida20",
        "aplication_name": "Privilegiados PVIDA20",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA169",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida169",
        "aplication_name": "Privilegiados PVIDA169",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA101",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida101",
        "aplication_name": "Privilegiados PVIDA101",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA74",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida74",
        "aplication_name": "Privilegiados PVIDA74",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA21",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida21",
        "aplication_name": "Privilegiados PVIDA21",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA113",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida113",
        "aplication_name": "Privilegiados PVIDA113",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA27",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida27",
        "aplication_name": "Privilegiados PVIDA27",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA254-1",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida254-1",
        "aplication_name": "Privilegiados PVIDA254-1",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA230-1",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida230-1",
        "aplication_name": "Privilegiados PVIDA230-1",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA49",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida49",
        "aplication_name": "Privilegiados PVIDA49",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA276",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida276",
        "aplication_name": "Privilegiados PVIDA276",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA19",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida19",
        "aplication_name": "Privilegiados PVIDA19",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA6",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida6",
        "aplication_name": "Privilegiados PVIDA6",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA90",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida90",
        "aplication_name": "Privilegiados PVIDA90",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA26",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida26",
        "aplication_name": "Privilegiados PVIDA26",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA137",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida137",
        "aplication_name": "Privilegiados PVIDA137",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA254-new",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida254-new",
        "aplication_name": "Privilegiados PVIDA254-new",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA 254-new",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida 254-new",
        "aplication_name": "Privilegiados PVIDA 254-new",
        "sub_carpeta": "privilegiados"
    },
    {
        "server": "PVIDA236-new",
        "query": "USE master; IF OBJECT_ID('tempdb..#Permisos') IS NOT NULL DROP TABLE #Permisos; CREATE TABLE #Permisos (ServerName SYSNAME, LoginName SYSNAME, Estado NVARCHAR(20), Scope NVARCHAR(30), DatabaseName SYSNAME NULL, UserName SYSNAME NULL, DefaultSchema SYSNAME NULL, RoleOrPermission NVARCHAR(128), PermissionType NVARCHAR(20)); INSERT INTO #Permisos SELECT @@SERVERNAME,p.name,CASE WHEN p.is_disabled=0 THEN 'Activo' ELSE 'Bloqueado' END,'Servidor',NULL,NULL,NULL,r.name,'Rol' FROM sys.server_principals p INNER JOIN sys.server_role_members rm ON p.principal_id=rm.member_principal_id INNER JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id WHERE p.type_desc IN ('SQL_LOGIN','WINDOWS_LOGIN','WINDOWS_GROUP') AND p.name NOT LIKE '##%' AND r.name='sysadmin'; DECLARE @sql NVARCHAR(MAX); SELECT @sql=(SELECT ' USE '+QUOTENAME(name)+'; INSERT INTO #Permisos SELECT DISTINCT @@SERVERNAME,sp.name,CASE WHEN sp.is_disabled=0 THEN ''Activo'' ELSE ''Bloqueado'' END,''Base de Datos'',DB_NAME(),dp.name,dp.default_schema_name,rdp.name,''Rol'' FROM sys.database_principals dp INNER JOIN sys.database_role_members drm ON dp.principal_id=drm.member_principal_id INNER JOIN sys.database_principals rdp ON drm.role_principal_id=rdp.principal_id INNER JOIN master.sys.server_principals sp ON dp.sid=sp.sid WHERE sp.type_desc IN (''SQL_LOGIN'',''WINDOWS_LOGIN'',''WINDOWS_GROUP'') AND sp.name NOT LIKE ''##%'' AND rdp.name IN (''db_owner'',''db_securityadmin'',''db_accessadmin'',''db_ddladmin'',''db_datawriter'');' FROM sys.databases WHERE state=0 FOR XML PATH(''),TYPE).value('.','NVARCHAR(MAX)'); EXEC sys.sp_executesql @sql; SELECT DISTINCT RTRIM(ServerName) AS ServerName, RTRIM(LoginName) AS LoginName, RTRIM(Estado) AS Estado, RTRIM(Scope) AS Scope, RTRIM(DatabaseName) AS DatabaseName, RTRIM(RoleOrPermission) AS RoleOrPermission FROM #Permisos WHERE RoleOrPermission IN ('sysadmin','db_owner','db_securityadmin','db_accessadmin','db_ddladmin','db_datawriter') ORDER BY LoginName,DatabaseName,RoleOrPermission",
        "export_name": "privilegiados_pvida236-new",
        "aplication_name": "Privilegiados PVIDA236-new",
        "sub_carpeta": "privilegiados"
    }
]